@agent.md

**PII context:** the real TV IP, serial and MAC for this machine live in
`~/.config/roku-monitor/env` (outside the repo) and any narrative
household notes in `pi.md` — gitignored, never committed. Committed files use
placeholders (`<tv-ip>`, `<serial>`) per agent.md.
